(function () {
    'use strict';
    angular.module('backbone').factory('programarActivServicio', programarActivServicio);

    programarActivServicio.$inject = ['$q', '$http', '$location', 'constantesBackbone'];

    function programarActivServicio($q, $http, $location, constantesBackbone) {
        return {
        };
    }
})();