(function () {
    'use strict';
    angular.module('backbone').factory('datosgeneralesServicio', datosgeneralesServicio);

    datosgeneralesServicio.$inject = ['$q', '$http', '$location', 'constantesBackbone'];

    function datosgeneralesServicio($q, $http, $location, constantesBackbone) {
        return {

        };
    }
})();