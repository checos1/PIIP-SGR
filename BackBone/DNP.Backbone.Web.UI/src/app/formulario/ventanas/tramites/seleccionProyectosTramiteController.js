(function () {
    'use strict';

    seleccionProyectosTramiteController.$inject = ['$scope', 'backboneServicios'];

    function seleccionProyectosTramiteController(
        $scope,
        backboneServicios
    ) {
        var vm = this;
    }

    angular.module('backbone').controller('seleccionProyectosTramiteController', seleccionProyectosTramiteController);

})();