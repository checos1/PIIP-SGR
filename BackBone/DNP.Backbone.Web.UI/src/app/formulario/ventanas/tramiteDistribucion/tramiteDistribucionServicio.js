(function () {
    'use strict';
    angular.module('backbone').factory('tramiteDistribucionServicio', tramiteDistribucionServicio);

    tramiteDistribucionServicio.$inject = ['$q', '$http', '$location', 'constantesBackbone', 'servicioFichasProyectos', 'utilidades', 'FileSaver'];


    function tramiteDistribucionServicio($q, $http, $location, constantesBackbone, servicioFichasProyectos, utilidades, FileSaver) {
        return {
            
        };

        
    }
})();