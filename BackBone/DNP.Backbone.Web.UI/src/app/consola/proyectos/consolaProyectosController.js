(function () {
    'use strict';

    consolaProyectosController.$inject = ['$scope',
        'constantesAutorizacion',
        '$uibModal',
        'FileSaver',
        'utilidades',
        'autorizacionServicios'];

    function consolaProyectosController($scope, constantesAutorizacion, $uibModal, FileSaver, utilidades, autorizacionServicios) {
        var vm = this;
    }

    // ReSharper disable once UndeclaredGlobalVariableUsing
    angular.module('backbone').controller('consolaProyectosController', consolaProyectosController)

})()