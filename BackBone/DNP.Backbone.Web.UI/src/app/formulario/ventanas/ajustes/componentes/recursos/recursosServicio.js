(function () {
    'use strict';
    angular.module('backbone').factory('recursosServicio', recursosServicio);

    recursosServicio.$inject = ['$q', '$http', '$location', 'constantesBackbone'];

    function recursosServicio($q, $http, $location, constantesBackbone) {
        return {

        };
    }

})();